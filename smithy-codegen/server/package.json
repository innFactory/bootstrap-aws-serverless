{
  "name": "typescript-gen",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "cdk": "dist/bin/cdk.js"
  },
  "scripts": {
    "pretest": "npm run build",
    "generate-ssdk": "cd ../ && ./gradlew :server-codegen:build",
    "build:ssdk": "cd codegen/build/smithyprojections/server-codegen/ts-server/typescript-ssdk-codegen && npm install --force && npm run build",
    "regenerate:ssdk": "npm run generate-ssdk && npm run build:ssdk",
    "build": "npm run regenerate:ssdk && tsc"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@aws-smithy/server-apigateway": "^1.0.0-alpha.5",
    "@aws-smithy/server-common": "^1.0.0-alpha.5",
    "@types/aws-lambda": "^8.10.85"
  }
}
